# Résumé - 08 Mai 2026

**1. Clé API OpenAI (Service Cloud API - Payant ‼️)**
- Erreur corrigée : Remplacement du chemin du fichier par la vraie clé secrète (`sk-proj-...`) dans `.env`.
- ‼️ L'utilisation de GPT-4o Vision coûte ~$0.01/page. Il faut minimum $5 de crédit sur le compte OpenAI.

**2. Moteur IA Ollama (Logiciel installé depuis site web - Gratuit)**
- Erreur corrigée : "Ollama non accessible".
- Solution : Lancement de la commande `ollama serve` en arrière-plan.

**3. Base de Données ChromaDB (Package Python - Gratuit)**
- ‼️ Pour supprimer la base rapidement : Éteindre le backend et supprimer les dossiers `backend/chroma_db/` et `backend/uploads/`.
- Via API : Route `DELETE /api/documents/{document_id}` sur Swagger UI.

**4. Commandes de Démarrage ‼️**
- **Ollama** (Logiciel local - Gratuit) : `ollama serve`
- **Backend FastAPI** (Package Python - Gratuit) : `.\venv311\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000`
- **Frontend React/Vite** (Package Node.js - Gratuit) : `npm run dev`
  - Lien de consultation de l'application : http://localhost:5173
  - Lien de consultation de l'API (Swagger UI) : http://localhost:8000/docs

**5. Lenteur au Démarrage**
- ‼️ Le backend prend ~1 minute au 1er lancement pour charger en RAM les modèles **BGE-M3** et **BGE-Reranker** (Packages Python/Modèles IA - Gratuits). Ensuite, c'est très rapide.
