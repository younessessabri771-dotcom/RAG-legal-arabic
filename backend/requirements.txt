# ===================================================
# RAG Bot - Documents Juridiques Arabes
# Requirements — Backend FastAPI
# ===================================================

# --- API & Serveur ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.7.0
pydantic-settings==2.3.0
python-dotenv==1.0.0

# --- Extraction & Traitement PDFs ---
pymupdf==1.24.0
Pillow==10.3.0
pdf2image==1.17.0

# --- GPT-4o Vision (PAYANT - clé API requise) ---
openai==1.35.0

# --- Normalisation Arabe ---
camel-tools==1.5.4
pyarabic==0.6.15

# --- Embeddings (BGE-M3 - Gratuit, Local) ---
sentence-transformers==3.0.0
torch==2.3.0
transformers==4.41.0

# --- Base Vectorielle ---
chromadb==0.5.0

# --- Reranker (BGE-Reranker - Gratuit) ---
FlagEmbedding==1.2.10

# --- LLM & Framework ---
langchain==0.2.0
langchain-community==0.2.0
langchain-core==0.2.0
langchain-groq==0.1.4

# --- Utilitaires ---
aiofiles==23.2.1
python-jose==3.3.0
httpx==0.27.0
