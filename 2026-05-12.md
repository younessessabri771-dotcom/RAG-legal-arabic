# 2026-05-12

## En bref

- **Correction d'affichage des sources** : Les chunks étaient tronqués. 
  - Backend : La limite de 300 caractères a été supprimée dans `chat.py`. Le texte complet du chunk est désormais envoyé au frontend.
  - Frontend : Ajout d'un système expand/collapse (cliquer pour agrandir/réduire) sur les cartes de sources dans `SourceCard.jsx` et `index.css`.

‼️ **Problème identifié (à résoudre)** : Le système retourne des chunks identiques/redondants dans les sources (même document, même page, même score, même texte répété 5 fois). Une déduplication des chunks doit être implémentée dans le backend avant le renvoi au frontend.
